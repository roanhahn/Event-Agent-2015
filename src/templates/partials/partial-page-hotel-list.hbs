<!--{{> event-hero }}-->
<section class="section section--grey">
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                {{> partial-ticket-form }}
            </div>
        </div>
        <div class="row">
            <div class="col-sm-6">
                <h3 class="fold-h3">MYSTERYLAND SELECTED HOTELS</h3>
            </div>
            <div class="col-sm-6 text-right">
                <ul class="switch-display clearfix">
                    <li class="display-options-map">Show on map</li>
                    <li class="display-options-list active">Show as list</li>
                </ul> 
            </div>
        </div>
        {{> partial-hotel-list-item }}
        {{> partial-hotel-list-item }}
        {{> partial-hotel-list-item }}
        {{> partial-hotel-list-item }}
        {{> partial-hotel-list-item }}
        {{> partial-hotel-list-item }}
        {{> partial-pager }}
    </div>
</section>